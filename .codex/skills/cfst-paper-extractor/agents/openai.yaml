interface:
  display_name: "CFST Paper Extractor"
  short_description: "Preprocess MinerU outputs and extract one-paper CFST structured data"
  default_prompt: "Use runtime-provided paths to preprocess parsed files when needed and extract one-paper CFST experimental data into validated Group_A/Group_B/Group_C JSON."
